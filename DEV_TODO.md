# Dev To-Do

This is the detailed backlog. For the shorter current-use/future-use roadmap, use `CURRENTUSE_FUTUREUSE.md`.

These are intentionally for after the current packet/signing flow is tested and corrected.

- Project location rule: do not create or keep active project folders on `C:\` or OneDrive. Use `F:\ONGOINGPROJECTS\...` for active projects and `E:\...` or `F:\...` for import/export packages unless Michelle explicitly says otherwise. Any `C:\...` path in old notes should be treated as a historical source reference only.
- Discuss how to host this portal on the self-hosted Pueblo, Colorado server that serves Edgewater, Florida over the internet.
- Use `BEGINNER_LINUX_SERVER_SETUP_GUIDE.md` as the single checklist for server software, commands, dynamic DNS, and security hardening.
- Discuss whether to run this directly on the Linux server or inside a virtual machine.
- Server hardware notes for later discussion: Acer Aspire A514-54, 11th Gen Intel i5-1135G7 @ 2.40 GHz, 8 GB RAM with about 7.6 GB usable, Intel Iris Xe graphics.
- Server location/use note: the self-hosted production server is expected to be in Pueblo, Colorado. It may also double as a light dev/studio machine, but Edgewater, Florida staff/customers will access it over the internet.
- Server OS note: Ubuntu Studio is already installed and should be used as the baseline unless there is a deliberate reason to reinstall.
- Dynamic DNS note: first use ISP/router built-in Dynamic DNS or static public IP if offered; otherwise use No-IP official updater. Hostinger DNS API script remains an alternate future option; DuckDNS is testing/backup only because reliability is not trusted enough for production.
- Public access decision note: ISP static/public IP has been requested. While waiting, research non-Cloudflare temporary access options: Tailscale Funnel, ngrok, localhost.run, or a small VPS reverse proxy. Free tunnels should be treated as testing/training only unless security is deliberately reviewed.
- Public access strategy note: use `PUBLIC_ACCESS_STRATEGY.md` for the proactive no-wait plan. Test direct IPv6, keep building the app on LAN, use Tailscale Funnel only for temporary testing if needed, and keep VPS reverse proxy as the fallback if the ISP cannot provide public IPv4.
- Printed guide note: use `SERVER_SETUP_PRINTED_GUIDE_CORRECTIONS_2026-05-23.md` for printout corrections instead of forcing a full reprint.
- Final setup manual polish: after the server setup content is tested/corrected, generate a clean printable manual with a table of contents/index, page headers, centered section headers, bottom-right page numbers, and write-in blanks near each section for values that must be remembered later.
- Final setup manual usability goal: the compiled manual should be clear enough that another person can install this server later without the chat history. Include plain-English explanations, exact command blocks, expected good results, common bad results, troubleshooting, restart commands, and notes like "use the first/non-temporary IPv6 address for DNS, not the temporary dynamic privacy address."
- Future product direction: finish and stabilize the Cabinet Store version first, then make the portal configurable/white-label so it can be tailored for other businesses without rewriting the core contract/signing/customer-file system.
- Future small-device pass: after the estimate/contract/installer workflows are working correctly, redesign the active portal screens for better phone/small-device use without changing the working business flow.
- Naming direction: use **Contract Portal** as the umbrella product name because customers, employees, installers, delivery drivers, and other roles may all receive contracts/forms to sign. Keep customer portal as the customer-facing role/view inside the larger Contract Portal.
- Server naming caution: the current Linux folder/service names still use `customerportal`. Do not rename those on the server until we intentionally plan a migration, because the current running service and setup guide depend on those paths.
- After the current deploy is healthy, run the security lockdown pass documented in `SECURITY_LOCKDOWN_PLAN.md`: HTTPS, production cookies, firewall ports, Nginx-only public access, private PostgreSQL/Node ports, secrets rotation, file permissions, backups, logs, and SSH safety.
- Review server security before production: HTTPS, admin authentication, session secrets, firewall/reverse proxy, backups, PDF storage access, SMTP secrets, audit logs, and update process.
- End-stage financial network requirement: before real financial systems go live, create a private access path for Edgewater sales/authorized staff only. Regular customers and public visitors must not be able to reach financial apps, database ports, admin panels, or internal APIs. Plan for VPN/private overlay or site-to-site access, user/device authorization, firewall allowlists, strong app login, audit logs, backups, and separate public/customer-facing routes from private financial routes.
- Decide on production database. Recommendation to discuss: PostgreSQL is likely the better long-term choice for the larger financial/customer/order system, unless the other modules are already standardized on MySQL.
- After UI/UX is complete and the backend works properly up to the migration point, import real business data from cleaned legacy-system files. These source files include old system data used for tax filing/history, so keep the import process careful, traceable, and backed up before changing anything.
- Build an admin-only import/prepopulate module. First priority is customers, suppliers/vendors, and products. Second priority is estimates, existing signed/scanned contracts, installers, and related documents. Use `IMPORT_PREPOPULATE_PLAN.md` as the working plan. Imports must stage records for review, detect duplicates, preserve source files, keep audit details, and never silently overwrite production records.
- Before importing real legacy data, create a database growth plan so the database does not explode as entries, documents, logs, signatures, receipts, acknowledgements, vendor files, and history are added. Decide what belongs in database tables versus file storage, how attachments are referenced, retention rules, indexing, archiving, backups, and cleanup/maintenance routines.
- Move admin users out of `.env` and into the database.
- Add an admin screen to change admin passwords.
- Add password-reset flow for admin users and future customer accounts.
- Store passwords as secure password hashes, never plain text.
- Add database tables for customers, users, orders, contracts, signatures, email history, uploaded receipts/acknowledgements, vendor invoices, vendor receipts, job costing files, and audit logs.
- Future storage direction: local-first is acceptable. A store computer or Edgewater-side local database can write locally first.
- Future sync direction: changed Edgewater-local records should sync to the Pueblo-hosted server after each change or on a short queued retry loop.
- Future sync direction: sync must happen regardless of where a contract is initiated or edited: admin desktop, store tablet, customer-facing link, installer portal, employee portal, delivery-driver device, or a future local/offline module. Track origin device/app, editor role, timestamps, revision number, and sync status for every change.
- Future reporting direction: reports on the Pueblo-hosted server should be able to read synced data in near real time without forcing every Edgewater store action through a live hosted transaction.
- Future sync safety: track record IDs, revision numbers, updated timestamps, deleted records, sync status, conflicts, retries, and which machine/user made the change.
- Build real customer records separate from contracts. Customer records should be searchable by name, phone, mailing address, billing address, email, and any identifying field.
- Customer lookup direction: when staff selects a customer, show related records grouped from the same customer file such as estimates, contracts, invoices, receipts, acknowledgements, vendor documents, and other attached documents. Search should begin as they type and should avoid forcing staff to know which module already holds the record.
- Build real contract records separate from customer records. Installation address belongs to the contract/job record.
- Add Create Contract workflow: choose New Customer or search/select Existing Customer before contract entry.
- Improve View/Edit Contract workflow: search, details, draft edit, and E-number revision creation now exist; next pass should make the view mode more polished and add side-by-side revision comparison.
- View/Edit screens should default to clean read-only view mode. Edit fields should only appear after clicking Edit, with Save/Cancel.
- After an order/contract has been accepted, the original contract cannot be overwritten. Basic E-number revision creation exists; future database version should enforce the same rule at the SQL transaction level.
- Signed-document rule: once a contract/document is signed, the customer can view/download/print it but cannot change it. Changes must happen through a new signed revision or signed addendum.
- Change-order/addendum future: define a dollar threshold for "big changes" that require a signed addendum/change order after contract signing. The customer contract language should be updated later to explain that significant changes require written/signed approval before work/order changes proceed.
- Registered customer future: once customers have real accounts, logged-in registered customers should see their contracts without re-entering each document password, but each contract should still display its packet password for reference and PDF access.
- Add contract history/audit comparison so original contract and E1/E2 edits can be compared side by side.
- Complete first-run setup flow: ask store rep whether to enter store information, logo, and signature; if yes, walk them through store details, logo upload, and representative signature upload.
- Consider optional customer accounts for easier signing on future purchases.
- If customer accounts are added, allow registered customers to change their portal password instead of only using the temporary packet password.
- Decide whether WordPress has any role. Current recommendation: keep this contract/customer portal in the custom Node app so signatures, PDFs, customer records, orders, payments, audit logs, and security stay together. WordPress may still be useful for the public marketing website, but it is probably not the best place for the signing workflow.
- Customer portal domain rule: customer-facing account/document access belongs at `customers.edgefam.com`. Staff contract entry stays on the contracts portal.
- Customer portal first active scope: customers should see only estimates assigned to them and contract pages/documents the store explicitly assigned for customer viewing/signing. Internal pages, unassigned packet sections, vendor/receiving/job-costing paperwork, and staff-only drafts stay hidden.
- Customer portal RFMS bridge scope: until this portal generates its own invoices/receipts, customer-visible account files can include assigned RFMS acknowledgements/invoices and assigned RFMS receipts. RFMS acknowledgements are the current order/invoice-style document; RFMS receipts are the current payment proof document.
- Future customer portal expansion: after invoices, payments, receipts, acknowledgements, and account status are built, customers can log in and see those assigned records from the same customer portal.
- Customer portal message rule: customers can message/email the store from inside the portal if they have questions or concerns. For now, those messages go to `edgewatercabinetstore@gmail.com`.
- Customer portal concern rule: if the customer sends a concern about an existing sale, the selected sale/invoice/order/estimate/contract number must be included in the email to the store when available.
- Customer portal account maintenance: customers should eventually be able to update email, mailing/job/billing address, second contact/name, phone numbers, and password. Do not overwrite old identity/contact values silently; keep prior emails, addresses, names, and phones as searchable aliases/history so staff can still find the same customer from older records.
- Customer portal account login: when a customer registers from an emailed estimate/contract document, default to the email address the document was sent to. Later Google login/OAuth can be added for customers, but public registration should also include bot protection such as CAPTCHA or another verification step before it is broadly exposed.
- Future customer file: allow acknowledgements, receipts, and related documents to be uploaded to the customer/order file.
- Accepted-order file workflow: once a customer accepts an estimate as an order, look for files named like `Acknowledgements` and attach them to the customer/order file.
- Accepted-order file workflow: once a customer accepts an estimate as an order, look for receipts and attach them to the customer/order file.
- Accepted-order file workflow: acknowledgements and receipts may come from the payment program/import folder, or the store rep should be prompted to manually attach them with a file picker.
- Simple paid-status bridge: after the assigned estimate/contract/RFMS acknowledgement/RFMS receipt flow works, add a lightweight payment tracker for Jamie. It should ask whether the order was paid, amount paid, payment method, payment date, reference/receipt number if available, notes, and whether to upload/attach the RFMS receipt. This is a bridge until the portal can create/print/email its own receipts.
- Receipt module first pass: start by asking amount, payment method, payment date, and receipt/reference number, then ask whether to upload the RFMS receipt PDF/image. Store the manual payment info and attachment together so the customer portal can show paid status and the receipt/proof when assigned.
- Customer order reconciliation requirement: custom cabinet jobs need income, customer documents, and receiving to match. Future reports should flag income without a matching customer/order, customer orders without expected payments, receiving without a customer/job link, customer jobs with payments but no receiving, and duplicate/suspicious receipt or receiving entries.
- Customer order reconciliation links: payment/income records should link to customer, estimate, contract/order, RFMS acknowledgement/invoice, receipt, and any native future invoice/payment records. Receiving/material records should link to the same customer/order/job when they are for that customer.
- Vendor/job-costing workflow: as vendor invoices are received, scan/upload or link them to the customer/order record.
- Vendor/job-costing workflow: as vendor receipts are received, scan/upload or link them to the customer/order record for job costing.
- Auto-fill rule: any data already generated, imported, uploaded, or provided elsewhere should automatically fill matching fields on existing contract forms.
- Paste/import UI rule: do not show raw paste-data fields for images such as logos or signatures. Use normal file upload or drawing tools for images. Reserve paste/import boxes for data that makes sense to paste, such as plain text, CSV, or structured import cleanup.
- Document identity rule: do not use final PDF page numbers as the permanent identity for documents because estimate PDFs, acknowledgements, receipts, scans, and multi-page attachments change page counts. Track documents by stable type/section keys such as estimate, acknowledgement, receipt, installer agreement, Exhibit A, delivery signoff, customer pickup release, vendor invoice, vendor receipt, and receiving paperwork.
- PDF assembly rule: template page numbers are only placement coordinates for the starter packet. The final assembled PDF should be built from ordered document/section records, then page numbers can be calculated after assembly for display only.
- PDF/live-preview standard: follow `PDF_OUTPUT_STANDARD.md`. Live previews are staff guidance only; save/download/print/email must use server-generated PDFs with normalized data and settings injected at generation time.
- Page 11 workflow: page 11 of the contract should fill in as vendor/estimate/order documents are attached or generated.
- Page 11 manual workflow: ask the store rep whether they want to print the form, handwrite it, and manually enter the data.
- Future scan-reading workflow: allow a scan of Page 11 or any other completed form page to be read/OCR-parsed and used to fill matching portal fields.
- Receiving form workflow: same as Page 11. Store rep can manually enter receiving data now; future version should allow scanning/reading the receiving form and filling the fields automatically.
- Receiving storage rule: material/receiving rows entered from the contract page are not throwaway page text. They must create/update real receiving records in the shared receiving pool, tagged with source `contract_page` and linked to the customer, contract, estimate, order, supplier, and entered-by user where known.
- Receiving duplicate-check rule: when staff enters or scans receiving from the main receiving area, compare against already stored contract-page receiving rows by supplier, PO/order/estimate/contract/customer links, product/item number, quantity, cost/amount, freight, and received date. If a likely duplicate exists, prompt staff to link/update the existing record, save a separate receiving record, or cancel/review.
- Google OAuth / Gmail / Drive TODO: add Google OAuth connection for a dedicated business mailbox and Drive/folder storage. As vendor emails arrive, detect customer name, order/invoice/estimate number, install address, supplier, PO number, acknowledgement number, receipt number, tracking number, and attached PDFs. Auto-apply high-confidence vendor acknowledgements, receipts, invoices, order confirmations, and tracking/delivery notices to the customer/order file they belong to. Queue uncertain matches for staff verification instead of attaching blindly.
- Product/estimate lookup direction: imported cabinet/countertop products and approved reusable installation line items should fuel estimate line-entry autocomplete. When staff types in a line, search product code, product name, supplier, item type, and description. If the item is not found, ask whether to save it as a future estimate suggestion. If staff says no, keep it in the estimate/customer file for processing but do not add it to reusable lookup suggestions.
- Products/AP final review rule: when reviewing products/AP against bank and credit-card support, anything marked as a sale but paid store-to-store must be treated as an inter-store inventory transfer, not a customer sale. Remove the amount from sales/customer income reporting, reduce inventory at the store the product was taken from, and record the receiving side or inter-store settlement when applicable. Until the businesses are consolidated into one store/inventory pool, the stores should pay each other for transferred product so the books, bank/card activity, and inventory movement match.
- RFMS audit/import safety direction: before merging legacy RFMS records, combine customer identity across stores where appropriate, suffix overlapping document numbers by store (`-F`, `-C`, `-A`, `-?`), and flag possible conflicts across sales, RFD/receipts, AP, purchase orders, vendor invoices, receiving, estimates, job cost, inventory, and order-profit records. Use store/year/month/type summaries to avoid collisions and double-counting during imports or reconciliations.
- Vendor ordering future: allow the portal to prepare vendor purchase orders from customer/order/product data, then submit by the safest vendor-supported method: official API/EDI when available, email with PDF/CSV attachment, portal upload/browser-assisted workflow, or manual checklist/open-vendor-site flow. Staff should review and approve before the system sends/submits an order.
- Future finance integration: connect this portal to the estimates and invoice modules when those are ready.
- Future finance security: estimates, invoices, payments, job costing, vendor costs, payroll/pay-history, and reporting should live behind the private Edgewater staff network, not on the public customer-facing portal path.
- Invoice/payment release guard: do not release native invoice/payment posting yet. Current scope should stop at independent estimates, independent contracts, and the explicit Start Contract From Accepted Estimate flow. RFMS acknowledgements/receipts can be attached as documents and payment proof, but native invoice/payment posting should wait until the financial workflow is ready.
- Future finance data rule: quotes, invoices, payments, refunds, voids, donations, warranty adjustments, and corrections must be append-only. Never overwrite the original quote/invoice. Store each change as a revision or event linked to the same quote/invoice number so reports can show both "what we knew at the time" and the current final state.
- Future finance paid-invoice change workflow: when an invoice already has payments, payouts, or other financial activity, staff must choose an add/remove/change/correction action instead of editing the paid invoice in place. The system should create a linked invoice revision, adjustment invoice, or credit memo, apply existing payments correctly, then show refund due, customer credit due, additional payment required, or paid in full. See `OWNER_PROCESS_CONTROLS_AND_FINANCE_TODO_2026-06-04.md`.
- Future finance audit rule: payment records must be separate linked records, not hidden by a later zero balance. Every status change should record who changed it, when it changed, what changed, and why. Required statuses/events include written, delivered, paid, refunded, voided, donated, warranty, adjusted, and corrected.
- Future cash target calculator: before native invoices/payments are released, add a controlled cash-target/cash-discount calculator. It should let authorized staff enter a desired cash total, calculate the customer-facing discount needed, record tax separately, show the discount to the customer, and put the transaction into a petty-cash/cash-drawer review queue for the superadmin/financial person. This is separate from supplier/dealer cost discounts, catalog multipliers, and internal margin/markup rules.
- Future petty-cash flow: cash-target transactions should appear in a report with customer/order/invoice reference, cash received, discount amount, tax amount, payment date, method, entered-by user, approval/review status, and reconciliation notes.
- Sales Portal future: once the estimate, invoice, payment, customer, and related day-to-day modules are finished, create a Sales Portal for daily store operations. The Contract Portal should remain focused on contract packets, signatures, finalized customer documents, and related contract flow instead of becoming the only daily operations screen.
- Entry-page future: keep the Contract Portal landing split by role: Customers, Installers, and Store. Customers should eventually have a normal login/register/customer-account path for repeat customers and builders, while still allowing direct contract approval without creating an account when appropriate.
- Customer login future: the staff login page should stay staff-focused. Customer lookup/login/register belongs under the Customers choice on the main portal entry page.
- Employee Portal future: after the customer contract flow is stable, build the Store/Employee portal for staff login, employee account basics, paystub/pay history access, contract setup, and delivery/sales workflow links.
- Installer Portal future: keep installer access hidden or inactive until built. For current use, installers can sign printed documents that are scanned and attached to the customer/order file.
- UI refinement: saved record data should display as clean read-only text by default. It should only become editable fields after the user clicks an Edit button or edit checkbox, then Save/Cancel should appear.
- UI refinement: continue moving packet entry into clear guided sections so employees are not overwhelmed by one long form.
- Entry/module page style: preserve the current admin entry layout/top menu. Use compact module cards with icon/title top rows, search first where relevant, then compact Add New/View/Print actions. Do not redesign this structure unless Michelle explicitly asks for a specific structural change.
- Staff entry-page future: consider a singular staff work area rather than separate large cards. A top search bar could search across customers, estimates, and contracts; staff can choose Estimate, Contract, or Customer File from a compact selector/action area. Keep quick access to Print Blank Contracts and Installer Photo View. Operations/receiving/products should eventually live under the Manage site instead of crowding the basic contracts/estimates staff entry page.
- Customer signing refinement: introduce the customer packet in steps instead of one long signing screen.
- Contract creation refinement: continue staging the packet. Stage 1 is Customer Information Sheet. Stage 2 asks whether the sales rep wants to print/complete the Quick Measurement form or skip it.
- Contract creation refinement: Step 3 is Sales Estimate. The sales rep should open CabQuotes2 when needed, then attach the generated estimate as page 3 by scan/upload, or record the source folder/file.
- Estimate source: RFMS local folder default is `C:\Users\edgew\Documents\RFMS_PDF`; estimate files should start with `Estimate`, while older RFMS/CabQuotes files may still start with `Quote`.
- Estimate source: CabQuotes/LavaCake estimates may live under `https://srv1543-files.hstgr.io/9b2a391b8c6f1838/files/nodejs/estimates/` for the `cabquotes2.edgewaterhomestores.com`/future LavaCake output flow.
- Future integration: decide how the portal is allowed to find generated estimate PDFs. If the portal runs on the same Windows machine, it can watch/read the local RFMS folder. If it runs on the Linux server, RFMS output needs a shared folder, upload step, sync job, or API/file index. The hosted CabQuotes/LavaCake folder needs an authenticated file listing, direct saved file path, or database handoff after estimate creation.
- Estimate email note: one sales estimate may already be emailed to the customer and store at the same time; future integration should attach/link that same estimate to the contract record automatically.
- Estimate module merge note: `F:\ONGOINGPROJECTS\EWCS_Q2E\public_html` is the read-only source for the copied module now under `/estimates/new`. Do not modify the original EWCS site when adjusting the portal copy.
- Estimate entry UI rule: staff should not have to see or edit store information while entering an estimate. Keep store name, address, contact, and logo in settings, and use those settings only on saved/downloaded/printed/emailed estimate PDFs. The portal copy now follows this rule.
- Vendor future: vendor/supplier records should include Edgewater sales-tax exemption certificate details and exemption number, so future POS/product/purchasing integrations can apply supplier tax-exemption handling correctly.
- Document storage future: first admin setup should ask where staff wants customer document folders saved. The server must keep a copy of every generated estimate, contract, signed packet, receipt, acknowledgement, and uploaded document so records are accessible from other machines and recoverable after a local workstation crash. Local access should be via shared folder/sync/mount, not by making the workstation the only source of truth.
- Portal separation rule: customer portal shows finalized customer-visible documents. Store/admin portal keeps the full workflow, source data, internal attachments, vendor/receiving/job-costing data, and edit history.
- Page 4 rule: page 4 must always be included in the final customer PDF and stored PDF.
- Customer-visible page rule: page 10 is for POS acknowledgements/customer receipts only. Do not use it for vendor invoices, vendor receipts, job orders, or internal receiving paperwork.
- Customer-hidden page rule: vendor invoices, vendor receipts, job orders to vendors/page 11, and receiving/material line paperwork/page 13 must stay internal and must not be included in the customer-facing PDF or customer portal.
- Customer final packet rule: signed installer agreements, Exhibit A when used, delivery signoff summaries, customer pickup releases, customer checklist pages, acknowledgements, receipts, and applicable delivery/pickup backup documents should be available to the customer as part of the final contract packet when they apply.
- Initial customer signing packet rule from the existing template: customer packet should include Estimate if present, Legal Disclaimers always, Purchase Agreement pages/sections always even when unsigned but labeled with signed/unsigned status, and Addendum A only if store-provided split-payment information exists.
- In-house document rule: customer information and Quick Measurement are store/internal documents, not part of the initial customer signing packet.
- Later customer packet rule: acknowledgements/receipts, Chain-of-Custody/Material Release, Installer Agreement with Delivery/Installation Checklist, Delivery Signoff Summary, Customer Pickup Release, customer checklist pages, and other finalized customer-facing documents become available as the flow progresses.
- Store/internal archive rule: store/vendor/internal archive can include all pages/documents, including vendor invoices, vendor receipts, job orders, and receiving/job-costing paperwork. Customer-facing packets must use the customer visibility rules above.
- Customer display rule: customer portal should show section/document names with status labels such as signed, unsigned/pending customer signature, signed by installer, or installer signature pending. Do not make customers infer status from page numbers.
- Temporary packet password rule: current staff-only password pattern is first 4 letters of customer last name + street/building number + last 4 phone digits. Keep that pattern out of customer-facing screens. Future database/customer-account version should support stronger per-packet or per-customer secrets, password resets, optional one-time codes, and better identity verification.
- Signing-link email safety: do not explain the temporary password pattern in customer emails. Prefer sending the link by email and having the store rep provide the password separately by verbal/phone/text/store-approved method.
- Digital signature agreement rule: customer must agree to electronic signing before the portal finalizes the signed PDF. If they do not agree, they must sign in store or on paper.
- Customer email permission rule: before sending a signing link, the store should have customer permission to email. That permission may be verbal during the sale now, but future customer/account modules should track who received permission, when, and by what method.
- Identity confidence future: discuss stronger proof that the signer is the customer, such as customer accounts, one-time codes, verified phone/email, staff-assisted in-store signing, and store camera coverage for in-store signing stations where appropriate.
- Pages 5 through 8 rule: agreement pages should be filled from entries where possible; missing items should allow entry before sending/printing. Store rep should review and approve pages 5-8 before sending or printing.
- Detachable Addendum A rule: after the initial customer information/signature contract section, prompt the store rep whether split payment Addendum A is needed. If yes, page 9 is included and filled out by a store rep. Customers can review/sign as needed, but customers cannot edit Addendum A.
- Store rep edit rule for Addendum A: any logged-in store rep can edit Addendum A before the contract is accepted; customers cannot edit it.
- Initial contract flow note: once Addendum A is handled or skipped, that ends the initial customer contract. Later customer pages should be prompted in the flow only when needed.
- Delivery-stage workflow: Chain of Custody / Material Release is separate from the initial contract and should appear in the customer portal when delivery/pickup is ready.
- Delivery-stage workflow: page 14 Chain-of-Custody is signed by Edgewater when material is received at the store.
- Delivery-stage workflow: if an installer picks up material, the installer should confirm they inspected/picked up the material.
- Delivery-stage workflow: if material is delivered to the customer, the driver can present the release on their phone/tablet or send/open the customer link when arriving.
- Delivery-stage workflow: the customer can be pre-sent the release link, but the customer release/signature should only become active after delivery/pickup is marked ready or delivered.
- Delivery-stage workflow: customer MUST sign the material release form upon delivery or pickup.
- Delivery-stage workflow: if the installer picks up or handles the delivery, both installer and customer signature portions are required on the applicable release/checklist.
- Delivery-stage workflow: if the customer picks up, use the customer pickup release and the customer checklist portion so the customer is clearly told what to inspect before accepting material.
- Delivery-stage workflow: if Edgewater's store delivery driver delivers, the driver/store-rep portion and customer delivery signoff portion should be used.
- Delivery-stage workflow: delivery and customer checklist pages at the end of the packet are selectable as needed and should be included in the final customer packet when they apply.
- Delivery-stage workflow: customer-facing portal should show Chain of Custody / Material Release status by order and allow the customer portion to be selected and signed digitally when ready.
- Delivery-stage workflow: Chain of Custody / Material Release can be printed or emailed to the customer by the customer from their portal when allowed.
- Delivery-stage workflow: Chain of Custody / Material Release can be printed or emailed to the customer by a store rep.
- Role access workflow: store reps, delivery drivers, and installers should all have portal access for their own portions of the sale, with role-limited screens and permissions.
- Role access workflow: driver/installer access should not expose unrelated customer/account/admin data.
- Customer opt-in workflow: for non-primary forms after the initial estimate/acknowledgement contract stage, the customer can opt into portal completion/email delivery, or the store rep can print and hand the form to the customer.
- Scan import workflow: forms that are printed/handwritten should be scanned and imported into the customer/order file; future OCR can read the scan and fill matching fields.
- Delivery-stage workflow: capture signer role, timestamp, IP/device/browser, and whether signature happened in store, at delivery, by driver-presented device, or by customer-opened link.
- Delivery-stage workflow: branch the workflow into store receiving, installer pickup/inspection, customer delivery signoff, and customer pickup material release so the customer is not shown irrelevant pages.
- Scope priority: primary focus remains perfecting the contract flow through estimate and acknowledgement. Delivery/chain-of-custody, receiving, vendor, and later forms should be forward-compatible but should not derail contract testing.
- Module planning: future internal modules should include Customers, Installers, Vendors/Receiving, purchasing/products, and other operations areas that should be searchable and action-driven from the module entry style.
- Customer signing refinement: if customer is in store, store rep can have the customer sign into the portal on store tablet/computer and draw with mouse/touch.
- Customer signing refinement: customer-facing signing should offer either step-by-step guided signing or full-document review with guidance to required initials/signatures.
- Customer account future: when full customer registration is added, move the captured email/text communication permissions into the customer account profile and keep a history of when each consent was granted or changed.
- Compliance review future: before any marketing email or marketing/account SMS is actually sent, review opt-in wording, opt-out handling, audit logs, and provider requirements.
- SMS/email follow-up future: when SMS is implemented and jobs reach the proper completed/finalized status, send an owner-approved satisfaction follow-up by email and/or SMS. If the customer is satisfied, ask for a Google review and/or Facebook recommendation/tag/review using approved links. Respect email/SMS opt-in, opt-out, quiet hours, and audit logging.
- Signature pad future: add support for a connected hardware signature pad for in-store/counter signing. Browser mouse/touch drawing already exists for customer signing and saved store representative signatures.
- Tablet packaging future: package the Contract Portal workflow for Android tablets and possibly iPad. Consider a dedicated store tablet in kiosk/proprietary mode that opens only the portal for signing, delivery, installer, and staff contract tasks.
- Phone app demand tracking: small phone screens should show a larger-device notice and a request form. If enough people request phone support, plan a phone app or phone-optimized mode.
- Data model note: customer record should hold customer/contact-level data such as name, phone, email, mailing address, and billing address. Installation address belongs to the contract/job/order because it can change between purchases.
- Current scope reminder: right now we only need to track the contract/signature workflow.
- Confirm in-store signing workflow during testing: customer signs digitally in the portal, then store prints their copy and emails it if requested.
