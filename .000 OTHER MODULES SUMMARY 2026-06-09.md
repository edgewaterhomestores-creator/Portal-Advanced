# Other Modules Summary - 2026-06-09

This file summarizes the portal modules and future modules outside the immediate contract-packet/signing work. It is meant for the next Codex session to read before starting new work.

Primary working root for this portal:

`F:\ONGOINGPROJECTS\CUSTOMERPORTAL_NEXT`

Nested Installer Portal source root:

`F:\ONGOINGPROJECTS\ContractsPortal\InstallerPortal`

Project-wide running log:

`F:\ONGOINGPROJECTS\ACTIVE_PROJECTS_RUNNING_LOG.md`

Do not use `C:\Users\miche\OneDrive\Documents` as an active project root.

## Current Overall Priority Order

Michelle's active priority order as of 2026-06-08 / 2026-06-09:

1. Stabilize and verify the current contract, estimate, customer signing, and staff login flow.
2. Clean up customer records so one customer does not split into duplicate active records when staff edits information or a customer registers/logs in.
3. Build installer accounts next.
4. After installer accounts exist, group installer details under the installer account/profile.
5. Add basic customer portal account information next:
   - assigned estimates,
   - invoices/acknowledgements,
   - payment proof or paid status,
   - signed contracts,
   - customer-visible documents.
6. Add a near-term paid-status bridge so Jamie/store staff can mark paid status, paid amount, and paid date.
7. Security lockdown is vital soon before installer/customer links are broadly shared.
8. Customer photo upload comes after customer portal basics are stable.
9. Receiving scans/uploads and vendor email/document processing come later, but should be built carefully because they need to attach to the correct customer/order.
10. UI layout cleanup is last, after working behavior is stable.

## Current Package / Deploy Status

- Latest created package at the time this file was written: Package 33.
- Standard upload file: `F:\customerportal-upload.tgz`
- Package 33 tracked copy: `F:\ONGOINGPROJECTS\CUSTOMERPORTAL_NEXT\POSTIMPORTPORTAL 33.tgz`
- Package 33 SHA256: `C7EBC5383440C9B13E9FF49B5078CA93061B63DDA09CECE17192FCBE80151C3A`
- Package 33 status at creation: created, not upload-confirmed yet.
- Package 32 status: deployed/upload-confirmed by Michelle at 2026-06-09 06:50 -06:00.

Use the standard copy/paste deploy flow only. Do not introduce new deploy scripts, one-line SSH deploy commands, alternate shells, or auto-opened windows unless Michelle explicitly approves it.

## Estimate Module

Current purpose:

- Estimate entry and saved-estimate handling for the Contract Portal.
- Current live route is under the Contract Portal at `/estimates/new`.
- This module is a copied/merged portal version of the estimate tool, not the original EWCS source. Do not edit the old EWCS source while changing this portal copy.

Current state:

- Staff can create an estimate.
- Store/business information fields are hidden from estimate entry.
- Store/business details, logo, contact info, and settings should come from portal settings when PDFs are generated.
- Staff can search saved estimates by customer, supplier, installer, estimate number, address, phone, line items, or other searchable text.
- Static saved-estimate dropdown was removed.
- Search results load from the server/database while typing.
- Selecting an estimate should load the record directly; user should not need a separate Load button.
- `New` and `Upload PDF` sit next to the search box.
- Package 33 aligns `New` and `Upload PDF` with the search input row.
- Existing estimate PDFs can be uploaded into the existing estimate PDF library.
- Estimate search has a database-backed API route.
- Estimate search/result script and CSS use cache-busting version tags so browsers pull the latest deployed assets.

Important recent fixes:

- Loaded estimates keep an edit anchor so changing the customer name saves back to the same estimate record.
- Local duplicate estimate identities are removed on save.
- Server estimate reads collapse duplicates by estimate number.
- Estimate sync, PDF generation, email, and PostgreSQL mirror saves use `estimateNumber` as a duplicate-prevention key.
- PostgreSQL stale duplicate `estimate_records` rows for the same estimate number are removed during save.
- Estimate Download/Print now uses one modal:
  - Download,
  - Print,
  - then in the same modal: New Estimate, To Contracts, Cancel to Home, Stay Here.

Known current rules:

- Estimate can exist independently without starting a contract.
- Contract can be created directly without an estimate.
- If an estimate is part of a contract flow, handoff must be explicit.
- An accepted estimate is supposed to come before creating a contract from that estimate.
- If an accepted estimate changes after acceptance, customer approval should normally happen again before contract creation.
- During troubleshooting, a store user can bypass with a note in a future flow.

Next estimate work:

- Enforce one accepted estimate per contract.
- Once an estimate has been converted/attached to a contract, do not allow that estimate to start another contract.
- Show already-converted estimates as attached, with a View Contract action instead of Start Contract.
- Add approval method UI for changed estimates or signed-contract changes:
  - Signed Change Order,
  - Email / Written Approval,
  - Bypass with note for troubleshooting.
- For Email / Written Approval, require email date, sent-from email, sent-to email, short summary, and a printable attachment or uploaded email copy.
- Improve multi-part customer names later with a confirmation step instead of guessing first/last name.

## Customer Records / Identity

Current purpose:

- Store customer-level information separately from contract/job/order-level information.
- Prevent duplicate active customer records for the same person.

Current state:

- There is customer lookup behavior from contracts and estimates.
- There are customer-related database records.
- Recent fixes removed stale customer lookup links when a contract is saved under a corrected customer name.
- Recent estimate fixes reduce stale duplicate estimate/customer names by preserving estimate identity.
- This is not yet a full customer master-record system.

Important data model rule:

- Customer record should hold customer/contact-level data:
  - name,
  - phone,
  - email,
  - mailing address,
  - billing address,
  - contact permissions,
  - aliases/history later.
- Installation address belongs to the contract/job/order because the installation address can change between purchases.

Known problem to solve next:

- If staff edits a customer name or customer details, the system needs to know whether it is:
  - a correction to the same customer,
  - a wrong customer selected and staff wants to switch to another existing customer,
  - a truly new/separate customer.
- Do not silently delete or overwrite older customer identity values.
- Keep older emails, names, phones, and addresses as searchable aliases/history where appropriate.
- Do not leave two active customers for the same real person unless staff intentionally chooses to save both.

Next customer-record work:

- Build a clearer customer identity/edit workflow.
- Add confirmation prompts when changing core name/contact fields.
- Link customer login/account identity back to the same customer file instead of creating a second record.
- Let staff search across all customer-related records without needing to know if the item is an estimate, contract, receipt, acknowledgement, or future invoice.

## Customer Portal / Customer Accounts

Current intended domain:

`customers.edgefam.com`

Current purpose:

- Customer-facing access to assigned customer-visible records and documents.
- Keep internal vendor, receiving, job-costing, and staff-only drafts hidden.

Current narrow first scope:

- Customers should see only:
  - assigned estimates from this system,
  - assigned contracts or contract pages/documents from this system,
  - assigned customer-visible external documents such as RFMS acknowledgements/invoices,
  - assigned RFMS receipts/payment proof,
  - signed contracts,
  - customer-visible final documents.

Current rules:

- Customers should not see internal pages, vendor invoices, vendor receipts, receiving paperwork, job costing, staff notes, or staff-only drafts.
- Customer portal should use document names and status labels, not page numbers.
- Customer should not have to re-enter a PDF password repeatedly once they have already logged in or verified the document.
- Customer-facing password screens should have Show/Hide controls.
- Customer final/thank-you page should offer print/download options after signing.

Current customer account direction:

- Customers may eventually register instead of using only a one-time signing link.
- Registration should default to the email address the estimate/contract was sent to.
- Customer usernames may be created from name/email later, but must be unique.
- If a person has more than one role/account type later, login should route them appropriately or ask which role/module to enter.
- Customer Google login/OAuth and bot protection are later public-account work.

Next customer portal work:

- Stabilize basic account information before adding photo upload.
- Show assigned estimates, signed contracts, RFMS acknowledgements/invoices, RFMS receipts, and paid status/proof.
- Allow customers to message/email the store with questions or concerns.
- If the concern is about an existing sale/order/estimate/contract, include that selected reference in the message to the store.
- Later allow account maintenance:
  - email,
  - mailing/job/billing address,
  - second contact/name,
  - phone numbers,
  - password.
- Keep old identity/contact values as searchable history/aliases.

Customer photo upload:

- Planned after customer portal basics are stable.
- Customers should eventually be able to upload photos by phone or computer.
- Customer photos should attach to the customer/order file.
- Do not build customer photos before the core customer account/customer record flow is stable.

## Staff / Admin / Users / Roles

Current purpose:

- Staff login and role-based access for store users.
- Keep user management restricted.

Current roles discussed:

- Staff / Store user.
- Admin.
- Superadmin.
- Finance role.
- Owner role.

Current rules:

- Superadmin has highest access.
- Admin can manage many staff tasks if granted.
- Only Superadmin can remove users.
- Superadmin can grant Admin/Superadmin roles, but Superadmin should be rare.
- Admin or Superadmin can send password reset links.
- Everyone should eventually be able to reset a forgotten password through an email link.
- Jamie should not manage other users unless she has an admin role; she should only manage herself by default.
- Michelle is supposed to be an admin in the portal context, but Michelle does not decide store business approvals; store reps do.

Staff login direction:

- Separate outdated staff login page was removed from active flow.
- Main entry login should route staff/customer/installer based on account type and destination.
- Staff username or email should work.
- If login fails, the user needs visible feedback:
  - wrong username,
  - wrong password,
  - disabled account,
  - missing account,
  - session/cookie issue.
- Staff Home from a logged-in page should go to the dashboard/module area, not back to login.
- Staff Logout should log out and go to `https://edgefam.com` where configured.

Signature security:

- A logged-in staff user must not create, edit, or apply another staff user's signature.
- Even admins should not sign for someone else.
- Store staff selected on an order should map to that user's own signature, not a dropdown of other people's signatures.
- Signature flow from a document should open a modal in context, not dump the user into an admin signature-management page.

Next staff/admin work:

- Move admin users out of `.env` and into the database.
- Store passwords as secure password hashes.
- Add admin screen to change passwords.
- Complete password reset flow for staff and customer accounts.
- Tighten role permissions before broadly sharing customer/installer links.
- Add audit logs for role/user/security-sensitive changes.

## Installer Accounts / Installer Details

Current separate app:

`F:\ONGOINGPROJECTS\ContractsPortal\InstallerPortal`

Current purpose:

- Installer photo upload and installer-facing workflows.
- Separate app/service currently runs under installer portal, nested under the Contract Portal container.

Current state:

- There is now an `installers` database table.
- Installer name dropdown exists in some portal contexts.
- Installer Portal photo uploads have been worked on:
  - camera/select existing,
  - per-photo review/removal,
  - upload progress,
  - attach-style button wording in some flows,
  - admin hard delete for temporary/test upload sets.
- Installer photos are stored under installer portal file storage and surfaced to staff/admin views.
- Superadmin/admin should be able to remove temporary/test upload sets.
- Installer photos should eventually connect to customer/order/job records, but that attachment workflow is not fully built yet.

Next installer work:

- Build installer accounts.
- Let installers log in and have their own default details autofilled.
- Installers should still be able to edit/change information if needed.
- Installer accounts should show their own uploaded image sets by date/job.
- Installer accounts should allow notes/name corrections.
- Later, job dropdowns should appear once jobs are actually assigned from the finance/order side.
- Later, installer can select assigned job and upload/attach photos directly to that job/customer file.

Installer future note:

- Future offspring idea: installers may want quick-send options to customers/vendors by text/email, wireless printing where available, and possibly cloud backup/storage.

## Vendor Documents / Vendor Payments / Receiving / Operations

Current purpose:

- Internal operational areas for vendor documents, vendor payments, receiving/materials, releases/support, and related store workflow.
- These are not production-ready official-record modules yet.

Current state:

- Vendor documents/payment, receiving/materials, and release/support sections have not-stable-yet notices.
- Staff should keep paper copies until Michelle tests and approves those sections.
- Receiving area shown under Operations is explicitly not tested yet.
- Receiving should show a contrasting notice that it is not tested.
- Receiving printouts should not print browser date placeholders or calendar icons.
- Printed receiving forms should have clear manual-entry boxes/lines if printed.
- There may be an existing receiving PDF form to map output to later.

Customer visibility rules:

- Vendor invoices, vendor receipts, job orders to vendors, receiving paperwork, and job-costing paperwork stay internal.
- These should not be included in customer-facing packets or customer portal unless explicitly converted into a customer-visible document.
- Page 10 is for POS acknowledgements/customer receipts only.
- Page 11/vendor order and Page 13/receiving-material paperwork are internal.

Receiving storage direction:

- Receiving/material rows entered from contract pages are not throwaway text.
- They must eventually create/update real receiving records in a shared receiving pool.
- Records should be tagged with source such as `contract_page`.
- They should link to customer, contract, estimate, order, supplier, and entered-by user when known.

Receiving duplicate-check direction:

- Later receiving scans/uploads/main receiving entries should compare against contract-page receiving rows by:
  - supplier,
  - PO/order/estimate/contract/customer links,
  - product/item number,
  - quantity,
  - cost/amount,
  - freight,
  - received date.
- If a likely duplicate exists, prompt staff to:
  - link/update existing record,
  - save a separate receiving record,
  - cancel/review.

Vendor email/document processing future:

- Add Google OAuth/Gmail/Drive for a dedicated business mailbox later.
- As vendor emails arrive, detect:
  - customer name,
  - order/invoice/estimate number,
  - install address,
  - supplier,
  - PO number,
  - acknowledgement number,
  - receipt number,
  - tracking number,
  - attached PDFs.
- Attach high-confidence vendor acknowledgements, receipts, invoices, order confirmations, and tracking/delivery notices to the correct customer/order file.
- Queue uncertain matches for staff verification.
- Do not attach uncertain vendor documents blindly.

Vendor payments wording:

- The current label `Vendor Payments` is confusing because staff needs to know whether it means customer paid us or Edgewater paid vendors.
- Future label should clarify this area means Edgewater/store payments to vendors, such as:
  - `Our Vendor Payments`,
  - `Payments to Vendor`,
  - or a clearer store-approved wording.

## Products / Suppliers / AP / Inventory

Current purpose:

- Future reusable product/supplier lookup for estimates, purchasing, receiving, AP, inventory, and reporting.

Current state:

- Estimate-created supplier names can mirror into supplier lookup records.
- Estimate line item autocomplete exists or is planned from imported products/reusable line items.
- Supplier field behavior is still being clarified:
  - supplier should be a supplier dropdown/list,
  - style/description should describe the product/order.

Product lookup direction:

- Imported cabinet/countertop products and approved reusable installation items should fuel estimate line-entry autocomplete.
- When staff types a line item, search:
  - product code,
  - product name,
  - supplier,
  - item type,
  - description.
- If item is not found, ask whether to save it as a future estimate suggestion.
- If staff says no, keep it only in the estimate/customer file.

Supplier future:

- Supplier records should include Edgewater sales-tax exemption certificate details and exemption number.
- Future purchasing/POS/product integrations should use supplier tax-exemption handling correctly.

AP / Inventory transfer rule:

- Products/AP final review with bank/credit card support needs special handling for store-to-store activity.
- Anything marked as a sale but paid store-to-store must be treated as an inter-store inventory transfer, not a customer sale.
- Remove the amount from sales/customer income reporting.
- Reduce inventory at the store the product was taken from.
- Record receiving side or inter-store settlement when applicable.
- Until the businesses are consolidated into one store/inventory pool, stores should pay each other for transferred product so books, bank/card activity, and inventory movement match.

## Finance / Invoice / Payment / Paid Status

Current release guard:

- Do not release native invoice/payment posting yet.
- Current usable scope should stop at:
  - independent estimates,
  - independent contracts,
  - explicit Start Contract From Accepted Estimate flow,
  - RFMS acknowledgements/receipts attached as customer-visible bridge documents.

Near-term paid-status bridge:

- After assigned estimates/contracts/RFMS documents work reliably, add a lightweight paid-status tracker.
- Jamie/store staff should be able to mark:
  - whether the order was paid,
  - amount paid,
  - payment method,
  - payment date,
  - reference/receipt number,
  - notes,
  - optional RFMS receipt upload/attachment.
- This is not the native finance module.
- It is a bridge so customer portal can show paid status/payment proof.

RFMS bridge:

- RFMS acknowledgements are the current order/invoice-style document.
- RFMS receipts are the current payment proof document.
- Customer portal can show assigned RFMS acknowledgements/invoices and RFMS receipts until native invoice/payment exists.

Future invoice/payment rules:

- Quotes, invoices, payments, refunds, voids, donations, warranty adjustments, and corrections should be append-only.
- Never overwrite the original quote/invoice.
- Store each change as a revision or event linked to the same quote/invoice number.
- Future reports need to answer:
  - what did we know at the time,
  - what is the current final state.

Paid-invoice change workflow:

- If an invoice already has payments/payouts/financial activity, staff must choose an add/remove/change/correction action.
- System should create a linked invoice revision, adjustment invoice, or credit memo instead of editing the paid invoice in place.
- Existing payments must be applied correctly.
- Show:
  - refund due,
  - customer credit due,
  - additional payment required,
  - paid in full.

Audit requirements:

- Payment records must be separate linked records.
- Do not hide payment history by later zero balance.
- Every status change should record:
  - who changed it,
  - when,
  - what changed,
  - why.
- Required future statuses/events include:
  - written,
  - delivered,
  - paid,
  - refunded,
  - voided,
  - donated,
  - warranty,
  - adjusted,
  - corrected.

Cash target calculator future:

- Before native invoice/payment release, add controlled cash-target/cash-discount calculator.
- Authorized staff can enter desired cash total.
- System calculates customer-facing discount needed.
- Tax is recorded separately.
- Discount is shown to customer.
- Transaction goes into petty-cash/cash-drawer review queue for superadmin/financial person.
- Keep this separate from supplier/dealer cost discounts, catalog multipliers, and internal margin/markup rules.

Financial security:

- Financial apps, database ports, internal APIs, reporting, invoices, payments, job costing, and payroll/pay-history should be behind private staff-only access.
- Public customers should not reach financial modules.
- Plan for VPN/private overlay or site-to-site access, device/user authorization, firewall allowlists, strong app login, audit logs, backups, and separate public/customer-facing routes.

## Import / Prepopulate / Legacy Data

Current purpose:

- Later admin-only import/prepopulate module for real business data.

Primary import priorities:

1. Customers.
2. Suppliers/vendors.
3. Products.
4. Estimates.
5. Existing signed/scanned contracts.
6. Installers.
7. Related documents.

Import rules:

- Use `IMPORT_PREPOPULATE_PLAN.md` as the working plan.
- Imports must stage records for review.
- Detect duplicates.
- Preserve source files.
- Keep audit details.
- Never silently overwrite production records.
- Real legacy source files must be treated carefully because they may support tax filing/history.

RFMS audit/import safety:

- Combine customer identity across stores where appropriate.
- Suffix overlapping document numbers by store such as `-F`, `-C`, `-A`, or `-?`.
- Flag possible conflicts across:
  - sales,
  - RFD/receipts,
  - AP,
  - purchase orders,
  - vendor invoices,
  - receiving,
  - estimates,
  - job cost,
  - inventory,
  - order-profit records.
- Use store/year/month/type summaries to avoid collisions and double-counting.

## Employee Portal / Sales Portal / Role Portals

Employee Portal:

- Future Employee Portal should come after the customer contract flow is stable.
- Planned use:
  - staff login,
  - employee account basics,
  - paystub/pay-history access,
  - contract setup links,
  - delivery/sales workflow links.
- Employee/payroll/financial access should be private staff-only, not public customer-facing.

Sales Portal:

- Future Sales Portal should become daily store operations once estimate, invoice, payment, customer, and related modules are ready.
- Contract Portal should remain focused on:
  - contract packets,
  - signatures,
  - finalized customer documents,
  - related contract flow.
- Daily store operations should not overload the contract workflow once a Sales Portal exists.

Delivery/Driver role:

- Future delivery drivers should have role-limited screens.
- Driver/installer access should not expose unrelated customer/account/admin data.
- Delivery workflows should branch into:
  - store receiving,
  - installer pickup/inspection,
  - customer delivery signoff,
  - customer pickup release.

## Security / Hosting / Backups

Current server naming caution:

- Current Linux app folder/service names still use `customerportal`.
- Do not rename server paths/services to `contractsportal` until a planned migration is approved.
- Current deploy and setup guide depend on those names.

Security lockdown:

- Use `SECURITY_LOCKDOWN_PLAN.md` after current deploy is healthy.
- Lock public access to HTTPS/Nginx.
- Keep Node and PostgreSQL private.
- Rotate temporary secrets.
- Verify backups.
- Protect SMTP secrets.
- Review audit logs and file permissions.
- Confirm firewall/reverse proxy rules.

Backups:

- Automatic 2-hour backups were discussed but deferred.
- Do not enable automatic 2-hour backups until Michelle says the current system is completely okay.
- Future backups must preserve live data and generated/uploaded documents.
- Do not back up test junk as official business backup if cleanup rules are not defined yet.

Deploy rule:

- Use existing copy/paste deploy steps only.
- After every `systemctl restart`, wait `sleep 5` before curl/status checks.
- Do not treat immediate curl connection refusal right after restart as proof of failure until after the pause/status check.
- If deploy fails, stop and report:
  - last completed step,
  - exact error,
  - what changed,
  - what did not change,
  - rollback path/command if known.

## UI / Layout / Stability Rules For Other Modules

Michelle's UI direction:

- Internal operations UI, not marketing site.
- Compact, professional, RFMS-style.
- Dense but readable.
- Avoid kindergarten/card-heavy/oversized layout.
- Avoid adding extra sections, helper text, design flourishes, or decorative containers unless requested.
- For records, use compact actions or row/action columns instead of bulky button bars.
- Use `View` for opening records/documents/images/PDFs/details.
- Use `Show` for reveal/expand/password visibility.

Stability notices:

- Receiving, vendor document/payment, release/support, and other not-yet-tested operational pages should warn staff to keep paper copies.
- Do not make placeholders look like active production tools.
- Do not expose future customer/invoice/payment dashboard items until they actually work.

Small-device future:

- Future small-device pass is planned after behavior is stable.
- Current priority is correctness and working flow.

## Recommended Next Session Startup

Open root:

`F:\ONGOINGPROJECTS\CUSTOMERPORTAL_NEXT`

First files to read:

1. `.000 SESSION SUMMARY CONTRACTS INSTALLERS 2026-06-09.md`
2. `.000 OTHER MODULES SUMMARY 2026-06-09.md`
3. `CURRENTUSE_FUTUREUSE.md`
4. `DEV_TODO.md`
5. `DEPLOY_COPY_PASTE_STEPS.txt`
6. `F:\ONGOINGPROJECTS\ACTIVE_PROJECTS_RUNNING_LOG.md`

Before starting new feature work, report the standing next tasks:

1. Confirm whether Package 33 was deployed.
2. If deployed, mark Package 33 upload-confirmed in the running log.
3. Test estimate search/loading/name correction/output flow.
4. Finish customer record cleanup/identity behavior.
5. Build installer accounts.
6. Build basic customer portal account information.
7. Add paid-status bridge.
8. Plan security lockdown.

Do not jump to another module before the active task is finished.
